import type { Metadata } from "next";
import { Inter, Outfit } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const outfit = Outfit({
  variable: "--font-outfit",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Traveloop — Personalized Travel Planning Made Easy",
  description:
    "Dream, design, and organize trips with ease. Create multi-city itineraries, track budgets, discover activities, and share travel plans with Traveloop.",
  keywords: [
    "travel planning",
    "itinerary builder",
    "trip planner",
    "travel budget",
    "multi-city trips",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${inter.variable} ${outfit.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
